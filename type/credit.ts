export type Balance = {
  creditBalance: number;
};

export type Options = {
  id: number;
  amount: number;
  basicCredit: number;
  bonusCredit: number;
  totalCredit: number;
  bonusRateText: string;
};
