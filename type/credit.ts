export type CreditBalance = {
  creditBalance: number;
};

export type CreditOptions = {
  id: number;
  amount: number;
  basicCredit: number;
  bonusCredit: number;
  totalCredit: number;
  bonusRateText: string;
};
